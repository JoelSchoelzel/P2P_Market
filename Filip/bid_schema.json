{
  "title": "ParsingModel[Bid]",
  "$ref": "#/definitions/Bid",
  "definitions": {
    "Time": {
      "title": "Time",
      "description": "The time of prediction for next hour, it can also identify the bids or transactions in hours.",
      "type": "object",
      "properties": {
        "time": {
          "title": "Time",
          "type": "string"
        }
      },
      "required": [
        "time"
      ]
    },
    "BuildingName": {
      "title": "BuildingName",
      "description": "To identify every building who submits the bid to coordinator, and coordinator can send the relevant transaction\naccording to their name.",
      "type": "object",
      "properties": {
        "name": {
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name"
      ]
    },
    "Price": {
      "title": "Price",
      "description": "The price of the buying or selling will be at first in bids provided from every building. But the final transacted\nprice will be by coordinator with multiple trading round determined.",
      "type": "object",
      "properties": {
        "price": {
          "title": "Price",
          "type": "number"
        }
      },
      "required": [
        "price"
      ]
    },
    "Quantity": {
      "title": "Quantity",
      "description": "Quantity will be at first in bids provided. After matching among buyers and sellers, appropriate matches will have\na certain quantity to exchange. One seller can sell its energy to one or more buyers.",
      "type": "object",
      "properties": {
        "quantity": {
          "title": "Quantity",
          "type": "number"
        }
      },
      "required": [
        "quantity"
      ]
    },
    "Role": {
      "title": "Role",
      "description": "This version of Role is currently only for implementing in bid",
      "type": "object",
      "properties": {
        "buyer": {
          "title": "Buyer",
          "type": "boolean"
        }
      },
      "required": [
        "buyer"
      ]
    },
    "BuildingID": {
      "title": "BuildingID",
      "description": "To identify every building who submits the bid to coordinator, and coordinator can send the relevant transaction\naccording to their ID number.",
      "type": "object",
      "properties": {
        "id": {
          "title": "Id",
          "type": "integer"
        }
      },
      "required": [
        "id"
      ]
    },
    "Bid": {
      "title": "Bid",
      "type": "object",
      "properties": {
        "timestamp": {
          "$ref": "#/definitions/Time"
        },
        "name": {
          "$ref": "#/definitions/BuildingName"
        },
        "price": {
          "$ref": "#/definitions/Price"
        },
        "quantity": {
          "$ref": "#/definitions/Quantity"
        },
        "buyer": {
          "$ref": "#/definitions/Role"
        },
        "number": {
          "$ref": "#/definitions/BuildingID"
        }
      },
      "required": [
        "timestamp",
        "name",
        "price",
        "quantity",
        "buyer",
        "number"
      ]
    }
  }
}